# MoodFlix configuration — override with environment variables (preferred) or edit locally.
# Copy application.properties.example for documentation of all keys.

db.host=localhost
db.port=5432
db.name=moodflix
db.user=postgres
# Set MOODFLIX_DB_PASSWORD environment variable instead of storing a password here.
db.password=

api.omdb.key=
api.unsplash.key=

app.seed.demo.data=true
app.dev.mode=false

http.connect.timeout.ms=5000
http.read.timeout.ms=8000
http.max.retries=2
